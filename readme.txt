To run frontend ->
prerequisite:
Angular should be installed on the laptop and npm also be installed.
Open the frontend folder on the terminal and enter the command ng serve

Note: if you are unable to run the project, simply delete folder node_modules and run the command nmp install before ng serve.
In the browser enter : http://localhost:4200/